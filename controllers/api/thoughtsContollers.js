const {Thought} = require("./../../models");

// Routes need to include create, update, and delete api calls

modue.exports = {
  // route to return all thoughts
  getThoughts(req,res) {
    
  },
  
  // route to get single thought
  getThought(req,res) {
    
  },

  // route to post reaction to a thought
  postReaction(req,res) {

  },

  // route to delete reaciton to a  thought
  deleteReaction(req,res) {

  }
};
